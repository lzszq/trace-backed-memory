{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/lzszq/trace-backed-memory/schemas/memory_revision_approval_v3.schema.json",
  "title": "Trace-backed Memory revision approval version 3 contract",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "contract_version",
    "approval_id",
    "revision_id",
    "memory_id",
    "revision_number",
    "previous_revision_id",
    "tenant_id",
    "repository_id",
    "artifact_content_sha256",
    "evidence_bundle_sha256",
    "approved_by",
    "approved_via_client_id",
    "authorization_event_id",
    "authorization_request_sha256",
    "authorization_policy_sha256",
    "approved_at",
    "approval_attestation_sha256"
  ],
  "properties": {
    "contract_version": {
      "const": "tbm.memory-revision-approval.v3"
    },
    "approval_id": {
      "pattern": "^memory_approval_sha256_[0-9a-f]{64}$",
      "type": "string"
    },
    "revision_id": {
      "$ref": "#/$defs/revisionId"
    },
    "memory_id": {
      "$ref": "#/$defs/identifier"
    },
    "revision_number": {
      "minimum": 1,
      "type": "integer"
    },
    "previous_revision_id": {
      "$ref": "#/$defs/nullableRevisionId"
    },
    "tenant_id": {
      "$ref": "#/$defs/identifier"
    },
    "repository_id": {
      "$ref": "#/$defs/nullableIdentifier"
    },
    "artifact_content_sha256": {
      "$ref": "#/$defs/digest"
    },
    "evidence_bundle_sha256": {
      "$ref": "#/$defs/digest"
    },
    "approved_by": {
      "$ref": "#/$defs/identifier"
    },
    "approved_via_client_id": {
      "$ref": "#/$defs/identifier"
    },
    "authorization_event_id": {
      "pattern": "^authz_sha256_[0-9a-f]{64}$",
      "type": "string"
    },
    "authorization_request_sha256": {
      "$ref": "#/$defs/digest"
    },
    "authorization_policy_sha256": {
      "$ref": "#/$defs/digest"
    },
    "approved_at": {
      "$ref": "#/$defs/timestamp"
    },
    "approval_attestation_sha256": {
      "$ref": "#/$defs/digest"
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "revision_number": {
            "const": 1
          }
        }
      },
      "then": {
        "properties": {
          "previous_revision_id": {
            "type": "null"
          }
        }
      },
      "else": {
        "properties": {
          "previous_revision_id": {
            "$ref": "#/$defs/revisionId"
          }
        }
      }
    }
  ],
  "$defs": {
    "identifier": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": ".*\\S.*"
    },
    "nullableIdentifier": {
      "oneOf": [
        {
          "$ref": "#/$defs/identifier"
        },
        {
          "type": "null"
        }
      ]
    },
    "digest": {
      "pattern": "^sha256:[0-9a-f]{64}$",
      "type": "string"
    },
    "revisionId": {
      "pattern": "^memory_revision_sha256_[0-9a-f]{64}$",
      "type": "string"
    },
    "nullableRevisionId": {
      "oneOf": [
        {
          "$ref": "#/$defs/revisionId"
        },
        {
          "type": "null"
        }
      ]
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(?:\\.[0-9]{1,6})?(?:Z|[+-](?:0[0-9]|1[0-4]):[0-5][0-9])$"
    }
  }
}
